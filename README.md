# C programming
This repo contains tasks relating to all things C -- compiling a program, using the standard library and working without the standard library as well; working with pointers, arrays, strings and using header files.

#include "main.h"

/**
 * main - Tests positive_or_negative() function to see if it holds up when the argument is 0.
 *
 * Return: Always 0 (success)
 */
int main (void)
{
	positive_or_negative(0);
	return (0);
}

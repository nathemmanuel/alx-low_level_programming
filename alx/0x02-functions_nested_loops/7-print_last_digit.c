#include <unistd.h>
#include "main.h"

/**
 * print_last_digit - prints the last digit of Agiven number
 * @l: The digit to be divided
 *
 * Return: the last digit of the number supplied
 */
int print_last_digit(int l)
{
	return ((l % 10));
}

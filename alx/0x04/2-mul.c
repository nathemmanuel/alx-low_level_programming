#include <unistd.h>
#include "main.h"

/**
 * mul - Multiplies the two arguments supplied
 * @a: one of the operands to be multiplied
 * @b: Another one of the operands to be multiplied
 *
 * Return: The value of the multiplication
 */
int mul(int a, int b)
{
	return ((a * b));
}

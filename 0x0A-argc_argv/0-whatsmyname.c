#include "main.h"
#include <stdio.h>

/**
 * main - prints the name of the executable file
 * @argc: Number count of the arguments used during compilation
 * @argv: Pointer to an array of strings
 *
 * Return: 0(success)
 */

int main(int argc, char *argv[])
{
	printf("%s\n", argv[0]);
	return (0);
}

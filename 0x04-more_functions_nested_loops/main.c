#include <stdio.h>

int main()
{
	char c = ' ';
	char t = 0;
	char a = 'k';

	int b = c + a;
	printf("%c\n", t);
	// every character has an integer value
	// Integer values up till 255 have corresponding character values 
	return (0);
}
